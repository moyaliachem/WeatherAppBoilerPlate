// import { selectWeatherAppContainerDomain } from '../selectors';

describe('selectWeatherAppContainerDomain', () => {
  it('Expect to have unit tests specified', () => {
    expect(true).toEqual(false);
  });
});
